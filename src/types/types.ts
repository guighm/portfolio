export type Language = {
    id: number,
    image: string
}

export type Project = {
    id: number,
    title: string,
    technologies: string[],
    link: string
}

export type Technology = {
    id: number,
    image: string
}